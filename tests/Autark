set {
  TESTS
  in-sources {
    ..@{autark glob '*.c'}
  }
}

cc {
  ${TESTS}
  set { _ ${CFLAGS} -I../../ }
  ${CC}
}

foreach {
  OBJ
  ${CC_OBJS}
  run {
    exec { ${CC} ${OBJ} -o %{${OBJ}} }
    produces {
      %{${OBJ}}
    }
    consumes {
      ${OBJ}
    }
  }
}