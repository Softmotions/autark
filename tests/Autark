set {
  TESTS
  ..@{autark glob '*.c'}
}

cc {
  ${TESTS}
  set { _ ${CFLAGS} -I../../ }
  ${CC}
}

run {
  exec { ${CC} ${OBJ} -o basename{${OBJ}} }
  produces {
    basename{${OBJ}}
  }
  consumes {
    foreach {
      OBJ
      ${CC_OBJS}
    }
  }
}