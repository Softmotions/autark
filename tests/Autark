set {
  TESTS
  ..@{autark glob '*.c'}
}

cc {
  ${TESTS}
  set { _ ${CFLAGS} -I../../ }
  ${CC}
}

foreach {
  OBJ
  ${CC_OBJS}
  run {
    exec { ${CC} ${OBJ} -o basename{${OBJ}} }
    produces {
      basename{${OBJ}}
    }
    consumes {
      ${OBJ}
    }
  }
}