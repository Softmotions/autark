
set {
  SOURCES
  in-sources {
    ..@{autark glob '*.c'}
  }
}

cc {
  ${SOURCES}
}

foreach {
  OBJ
  ${CC_OBJS}
  run {
    exec { ${CC} ${OBJ} -o basename{${OBJ}} }
    produces {
      basename{${OBJ}}
    }
    consumes {
      ${OBJ}
    }
  }
}